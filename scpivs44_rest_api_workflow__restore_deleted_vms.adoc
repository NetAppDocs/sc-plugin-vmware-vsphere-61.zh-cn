---
sidebar: sidebar 
permalink: scpivs44_rest_api_workflow__restore_deleted_vms.html 
keywords:  
summary: 要使用适用于SnapCenter Plug-in for VMware vSphere对虚拟机备份执行还原操作，必须遵循规定的 REST API 调用顺序。 
---
= 用于恢复已删除虚拟机的 REST API 工作流
:hardbreaks:
:allow-uri-read: 
:nofooter: 
:icons: font
:linkattrs: 
:imagesdir: ./media/


[role="lead"]
要使用适用于SnapCenter Plug-in for VMware vSphere对虚拟机备份执行还原操作，必须遵循规定的 REST API 调用顺序。

对于每个 REST API，添加 `\https://<server>:<port>`在 REST API 的前面形成一个完整的端点。

|===
| 步骤 | REST API | 注释 


| 1 | 前往 `\http://<vCenter-IP>/mob` | 从 VMware Managed Objects URL 中查找数据存储库 *moref*。请注意已删除虚拟机所在数据存储库的 moref。 


| 2 | `/4.1/datastores/{moref}/backups` | `Get Datastore Backups` 检索指定数据存储区的备份列表。传递步骤 1 中的 *moref*。记下要还原的备份的 *backupId*。 


| 3 | `/4.1/backups/{backupId}` | `Get Backup Details` 检索指定备份的详细信息。请传递步骤 2 中的 *backupId*。在实体列表中，找到与已删除虚拟机匹配的 *entityName*，并记录 *UUID*。 


| 4 | `/4.1/vm/backups/{backupId}/snapshotlocations` | `Get snapshot locations` 检索指定备份的快照位置。传递第 2 步中的 *backupId*。请注意 *snapshotLocationsList* 信息。 


| 5 | `/4.1/datastores/{moref}/availableEsxHosts` | `Get available ESX Hosts` 检索存储备份的主机的信息。传递步骤 1 中的 *moref*。请注意 *availableEsxHostsList* 信息。 


| 6 | `/4.1/vm/{uuid}/backups/{backupId}/restoreVm` | `Restore a VM from a backup` 还原指定的备份。传递步骤 3 中的 *UUID*。传递步骤 2 中的 *backupId*。在 *restoreLocations* 属性中，传递步骤 4 中 *snapshotLocationsList* 的信息；在 *esxHost* 属性中，传递步骤 5 中 *availableEsxHostsList* 的信息。如果虚拟机备份是部分备份，则将 `restartVM`参数设置为 *false*。*注意：* 您无法还原作为模板的虚拟机。 
|===